# CRM Timeline

Read-only Directus presentation interface for `crm_contacts.activity_timeline`.
It fetches the current contact's `crm_activities` and renders a Polish,
month-grouped timeline with filters, event-specific icons and expandable
details.

The interface requires read access to `crm_activities`. It never creates or
updates CRM records.

